# Test-only environment overrides. Never use these values in production.
# The testenv harness overrides DATABASE_URL/SECRET_KEY at runtime as well;
# this file exists for documentation and for running the app manually in test mode.
DATABASE_URL=sqlite:///./pharma_db_test.sqlite
SECRET_KEY=test-secret-key-for-testing-only
EXPIRY_ALERT_DAYS=30